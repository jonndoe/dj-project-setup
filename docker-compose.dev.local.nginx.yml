version: '3'


services:
  nginx:
    network_mode: "host"
    build: ./compose/local/nginx
    container_name: nginx
    volumes:
      - ./media/:/usr/src/media/
      - ./nginxlogs/:/var/log/nginx
